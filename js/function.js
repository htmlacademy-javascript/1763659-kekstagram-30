function getStringLength (string, maxLength) {
  if (string.length <= maxLength) {
    return true;
  }
  return false;
}

string('asdadasda3');
maxLength(20);

console.log('Длина строки ' + getStringLength + ' символов');

function polindromString (string) {
return(string.replaceAll());
return(string.toUpperCase());
}
let string = clearString;

string('Лёша на полке клопа нашёл ');
